{
  "name": "database-frontend",
  "version": "0.1.0",
  "description": "A simple but powerful database frontend",
  "scripts": {
    "build": "mkdir -p dist && browserify inc/app.js -o dist/app.js",
    "lint": "standard --fix inc/*.js page/*.js",
    "test": "phpunit test/test.php",
    "watch": "mkdir -p dist && watchify --debug inc/app.js -o dist/app.js -v"
  },
  "repository": "https://github.com/plepe/database-frontend/",
  "author": "Stephan Bösch-Plepelits <skunk@xover.mud.at>",
  "license": "GPL-3.0",
  "devDependencies": {
    "browserify": "^16.5.0",
    "standard": "^14.3.3",
    "watchify": "^3.11.1"
  },
  "dependencies": {
    "async": "^3.2.0",
    "html-escaper": "^3.0.0",
    "query-string": "^6.11.1",
    "sheet-router": "^4.2.3",
    "twig": "^1.15.0"
  }
}
